# Pacote 1 — estrutura visual e navegação

Data: 2026-09-07
Branch local: `codex/especificacao-mestra-2026-09-07`

## Entregue

- cabeçalho consistente no painel legado e no shell React;
- busca global de clientes por nome ou telefone, com cidade e telefone para
  distinguir homônimas;
- abertura da ficha pela identidade forte `clienteId`;
- deep-link do resultado React para a ficha operacional no painel legado;
- perfil identificado como placeholder “Em breve”, sem login ou permissões
  fictícias;
- navegação principal e subnavegação em linhas distintas;
- remoção da ação grande “Nova revendedora” repetida na faixa de resumo; a
  ação `+ Nova` da subnavegação e o fallback do estado vazio foram preservados.

## Testes

| Verificação | Resultado |
|---|---|
| `python src/build.py` | passou |
| `frontend/npm test` | 190/190 testes, 16/16 arquivos |
| `frontend/npm run build` | passou, 96 módulos |
| `node src/pacote1-shell-test.mjs` | passou |
| busca por nome e telefone | passou |
| seleção por teclado e abertura por ID | passou |
| deep-link React → legado | passou |
| responsividade em 390 px | passou, sem rolagem horizontal |
| console do navegador | zero erros |
| `GET /api/estoque/conferir` | `divergentes: 0` |

## Validação visual

As quatro capturas aprovadas estão em `pacote1-2026-09-07/`:

- `legado-desktop-busca.png`;
- `legado-mobile.png`;
- `react-desktop-busca.png`;
- `react-mobile.png`.

A revisão manteve a identidade visual aprovada — marfim, bordô, rosé e a
tipografia da marca — com utilidades discretas no topo. Busca e perfil não
competem com os módulos, e a navegação contextual permanece imediatamente
abaixo da navegação principal.

Toda a validação ocorreu localmente. Não houve deploy nem acesso a D1 remoto.
